%%SELECTOR%% {
  {{ macros.simpleLayout(design.layout.simplelayout, breakpoint) }}
  gap: {{ design.layout.gap.style }};
  width: {{ design.layout.width.style }};
  {{ macros.typography(design.typography, globalSettings) }}
}
  
%%SELECTOR%% > *:after { 
  content: "{{ content.data.separator }}";
  margin-left: {{ design.layout.gap.style }};
}