%%SELECTOR%% { 
	width: {{ design.container.width.style }};
  {{ macros.spacing_padding_all(design.container.padding) }}
  background-color: {{ design.container.background }}
  {{ macros.typography(design.container.typography, globalSettings) }}
}

.jv-wrapper h2 {
  {{ macros.typography(design.typography.h2, globalSettings) }}
}

.jv-wrapper h3 {
  {{ macros.typography(design.typography.h3, globalSettings) }}
}

.jv-page-body {
  {{ macros.typography(design.typography.global, globalSettings) }}
}

.jv-wrapper a {
 {{ macros.typography(design.typography.links, globalSettings) }}
}